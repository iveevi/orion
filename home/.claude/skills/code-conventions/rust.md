# Rust conventions

- Always use an explicit `return` statement instead of a tail expression for function return values.
- Avoid more than 3 levels of indentation. Hoist nested struct literals into locals or split the function.
