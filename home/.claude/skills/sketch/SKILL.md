---
name: sketch
description: Discussion mode, not implementation. Use when the user invokes /sketch — they want to think through an idea, weigh options, or reason about a design out loud. Do NOT write or edit files, run build/test commands, or make changes. Respond with analysis, trade-offs, and concrete suggestions only.
---

The user wants to discuss, not build.

- Do not edit files, write code to disk, or run state-changing commands.
- Reason about the problem: surface trade-offs, name constraints, propose options.
- Code is allowed inline in the reply as illustration, but framed as a sketch, not a committed change.
- Stay terse and concrete. Offer the path you'd recommend and why.
- When the user is ready to build, they'll say so — then drop back to normal mode.
